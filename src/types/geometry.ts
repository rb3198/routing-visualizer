/**
 * Defines a point on the grid. `[<x_coordinate>, <y_coordinate>]`
 */
export type Point2D = [number, number];
export type RectPoints = [Point2D, Point2D, Point2D, Point2D];

export type RectDim = { w: number; h: number };
