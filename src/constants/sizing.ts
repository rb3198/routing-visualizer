export const DEFAULT_CELL_SIZE = 48;
/**
 * Default size of area in # of cells.
 */
export const DEFAULT_AREA_SIZE = 7;

/**
 * Minimum possible zoom level
 */
export const MIN_ZOOM = 0.7;
/**
 * Maximum possible zoom level
 */
export const MAX_ZOOM = 1.3;
